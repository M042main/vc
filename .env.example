# Server-only Gemini credential. Configure this as a secret in deployment.
GEMINI_API_KEY=
# Optional server-only administrator override. Without these values, the fixed
# classroom code m042 works immediately. When overriding the code, also set an
# independent secret of at least 32 characters.
ADMIN_ACCESS_CODE=
ADMIN_SESSION_SECRET=
