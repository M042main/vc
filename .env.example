# Server-only Gemini credential. Configure this as a secret in deployment.
GEMINI_API_KEY=
# Required server-only administrator password. Set this as a Netlify secret.
ADMIN_ACCESS_CODE=
# Optional independent signing secret of at least 32 characters. When omitted,
# the server derives its session signing key from ADMIN_ACCESS_CODE.
ADMIN_SESSION_SECRET=
